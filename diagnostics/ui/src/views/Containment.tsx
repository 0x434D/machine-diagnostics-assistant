/** §7.2's containment view: the affected set, with an export. Built by M6 Task 7. */
import { Placeholder } from "./Placeholder";

export function Containment() {
  return (
    <Placeholder title="Containment" task="M6 Task 7">
      <p>
        The affected set for a query, with an export that is the set the query
        actually returned and carries its own count.
      </p>
    </Placeholder>
  );
}
