/** §7.2's stop timeline: station states as a Gantt across the window, with §5.4's
 * propagation chain drawn on it. Built by M6 Task 6. */
import { Placeholder } from "./Placeholder";

export function StopTimeline() {
  return (
    <Placeholder title="Stop timeline" task="M6 Task 6">
      <p>
        Station states as bars across the window, and the propagation chain
        drawn on the same axis, so §5.4&apos;s derivation is visible rather than
        asserted. The colours it will use are in the legend below.
      </p>
    </Placeholder>
  );
}
